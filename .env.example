# ── App secrets ───────────────────────────────────────────────
JWT_SIGNING_KEY=change-me-in-prod
POSTGRES_PASSWORD=postgres
GRAFANA_PASSWORD=admin
VAULT_ROOT_TOKEN=root

# ── LLM providers (at least one required for M2+) ─────────────
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# ── Portal ────────────────────────────────────────────────────
NEXT_PUBLIC_API_URL=http://localhost:8080

# ── Email (Mailhog in dev; real SMTP in prod) ─────────────────
SMTP_HOST=mailhog
SMTP_PORT=1025
SMTP_FROM=hello@aisdlc.local
